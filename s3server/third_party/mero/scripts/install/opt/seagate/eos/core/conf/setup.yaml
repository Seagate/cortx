core:
    post_install:
        script: /usr/sbin/m0provision
        args:
            - post_install
    init:
        script: /usr/sbin/m0provision
        args:
            - init
    config:
        script: /usr/sbin/m0provision
        args:
            - config
    test:
        script: /usr/sbin/m0provision
        args:
            - test
    reset:
        script: /usr/sbin/m0provision
        args:
            - reset
support_bundle:
    - /opt/seagate/eos/core/libexec/m0reportbug-bundler
