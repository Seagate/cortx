First, you have to install Python development package to build a module:

    sudo yum install python-devel

Second, build Mero as described in Mero README.

Third, build and install the mero module:

    sudo python setup.py install

To get help, execute the following:

    ./m0spiel --help
